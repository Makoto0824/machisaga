# Vercel KV (Redis) 設定
# Vercelダッシュボードの「Storage」→「KV」で作成後、以下を設定

# KV REST API URL
KV_REST_API_URL=https://your-kv-database-url.upstash.io

# KV REST API Token (読み書き用)
KV_REST_API_TOKEN=your-kv-rest-api-token

# KV REST API Read Only Token (読み取り専用)
KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token

# アプリケーション設定
NODE_ENV=production
